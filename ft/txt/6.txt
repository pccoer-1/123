
## 🔹 What is Dataset (Iris)?

Iris dataset = flowers data

Each row = one flower
Each column = measurement

| Feature     | Meaning              |
| ----------- | -------------------- |
| SepalLength | length of outer leaf |
| SepalWidth  | width of outer leaf  |
| PetalLength | length of inner leaf |
| PetalWidth  | width of inner leaf  |
| Species     | flower type          |

---

## 🔹 What is Classification (again)?

👉 Predict category

Here:

```text
Iris-setosa
Iris-versicolor
Iris-virginica
```

---

## 🔹 What is Naive Bayes?

👉 Classification algorithm based on probability

👉 Assumption:

```text
Features are independent
```

---

# ✅ 2. WHAT THIS EXPERIMENT DOES

### 🎯 Aim:

1. Visualize data
2. Understand feature relationships
3. Train Naive Bayes model
4. Classify flower types
5. Evaluate performance

---

### 🌍 Real-world Use:

* Email spam detection
* Medical classification
* Pattern recognition

---

# ✅ 3. FULL CODE (WORD-BY-WORD EXPLANATION)

---

# 🔹 Upload File

```python
from google.colab import files
uploaded = files.upload()
```

* Upload dataset

---

```python
if uploaded:
    print("Success")
else:
    print("Error")
```

👉 Check upload

---

# 🔹 Import Libraries

```python
import numpy as np
```

👉 Math operations

---

```python
import pandas as pd
```

👉 Data handling

---

```python
import matplotlib.pyplot as plt
```

👉 Graphs

---

```python
import seaborn as sns
```

👉 Advanced graphs

---

# 🔹 Load Dataset

```python
iris = pd.read_csv('Iris.csv')
```

👉 Load data

---

```python
iris.head()
```

👉 First rows

---

```python
iris['Species'].unique()
```

👉 Unique classes:

```text
Iris-setosa
Iris-versicolor
Iris-virginica
```

---

# 🔹 Drop Column

```python
iris.drop(columns="Id", inplace=True)
```

### Word-by-word:

* `drop` → remove column
* `Id` → not needed
* `inplace=True` → change original data

---

# 🔹 Visualization (IMPORTANT)

---

## 🔹 Scatter Plot

```python
g = sns.relplot(x='SepalLengthCm', y='SepalWidthCm', data=iris, hue='Species', style='Species')
```

### Meaning:

* `relplot` → relationship plot
* `x` → X-axis
* `y` → Y-axis
* `hue` → color by category
* `style` → shape by category

👉 Shows separation of species

👉 From *page 1 image*:
👉 Setosa is clearly separated

---

```python
g.fig.set_size_inches(10,5)
plt.show()
```

👉 Adjust size + display

---

# 🔹 Violin Plot (VERY IMPORTANT)

```python
sns.violinplot(x='Species', y='PetalLengthCm', data=iris)
```

👉 Shows distribution

---

### From *page 2 image*:

👉 Each species has different shape
👉 Helps see spread + density

---

Repeat for:

* PetalWidth
* SepalLength
* SepalWidth

---

# 🔹 Combined Plot

```python
sns.violinplot(data=iris)
sns.swarmplot(data=iris)
```

👉 Shows:

* distribution + individual points

---

# 🔹 Area Plot

```python
iris.plot.area(y=['SepalLengthCm','SepalWidthCm','PetalLengthCm','PetalWidthCm'])
```

👉 Shows trends over data

👉 From *page 3 image*:
👉 Petal features dominate

---

# 🔹 Correlation

```python
iris.drop(columns=['Species']).corr()
```

👉 Correlation matrix

---

### From *page 4 image*:

| Feature                  | Relation    |
| ------------------------ | ----------- |
| PetalLength & PetalWidth | HIGH (0.96) |
| SepalWidth               | weak        |

👉 Important insight

---

# 🔹 Heatmap

```python
sns.heatmap(..., annot=True)
```

👉 Visual correlation

---

# 🔹 Prepare Data

```python
X = iris.iloc[:, 0:4].values
```

👉 Features

---

```python
y = iris.iloc[:, 4].values
```

👉 Target

---

# 🔹 Encode Target

```python
from sklearn.preprocessing import LabelEncoder
le = LabelEncoder()
y = le.fit_transform(y)
```

👉 Convert:

```text
setosa → 0
versicolor → 1
virginica → 2
```

---

# 🔹 Train-Test Split

```python
from sklearn.model_selection import train_test_split
```

---

```python
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.3, random_state=0)
```

👉 70% train, 30% test

---

# 🔹 Model

```python
from sklearn.naive_bayes import GaussianNB
```

---

```python
gaussian = GaussianNB()
```

👉 Create model

---

# 🔹 Train

```python
gaussian.fit(X_train, y_train)
```

👉 Learn patterns

---

# 🔹 Predict

```python
Y_pred = gaussian.predict(X_test)
```

👉 Predict classes

---

# 🔹 Confusion Matrix

```python
cm = confusion_matrix(y_test, Y_pred)
```

---

### Output:

```text
[[16 0 0]
 [0 18 0]
 [0 0 11]]
```

---

### Meaning:

👉 Perfect classification

---

# 🔹 Metrics

```python
accuracy_score
precision_score
recall_score
f1_score
```

👉 All near **1.0 (100%)**

---

# ✅ 4. CORE CONCEPTS (CLEAR)

---

## 🔹 Naive Bayes Formula

```text
P(A|B) = P(B|A) * P(A) / P(B)
```

---

## 🔹 Independence Assumption

Features independent

---

## 🔹 Correlation

* +1 → strong relation
* 0 → no relation
* -1 → negative

---

## 🔹 Visualization Importance

👉 Helps understand data patterns

---

# ✅ 5. OUTPUT UNDERSTANDING

---

### From graphs:

* Setosa clearly separable
* Versicolor & Virginica slightly overlap

---

### From model:

```text
Accuracy = 100%
```

👉 Perfect classification

---

# ⚠ Common Mistakes

1. Not encoding labels
2. Wrong feature selection
3. Ignoring visualization
4. Misinterpreting correlation
5. Not splitting data

---

# ✅ 6. VIVA QUESTIONS (DETAILED)

---

### 1. What is Naive Bayes?

Probabilistic classifier.

---

### 2. Why called naive?

Assumes independence.

---

### 3. What is GaussianNB?

Continuous data version.

---

### 4. What is correlation?

Relationship between variables.

---

### 5. What is heatmap?

Visual correlation matrix.

---

### 6. What is scatter plot?

Shows relation between variables.

---

### 7. What is violin plot?

Shows distribution.

---

### 8. What is swarm plot?

Shows individual points.

---

### 9. What is LabelEncoder?

Converts text → numbers.

---

### 10. What is classification?

Predict categories.

---

### 11. What is accuracy?

Correct predictions ratio.

---

### 12. What is confusion matrix?

Performance table.

---

### 13. What is feature?

Input variable.

---

### 14. What is target?

Output variable.

---

### 15. What is independence assumption?

Features unrelated.

---

### 16. Why visualization needed?

Understand data patterns.

---

### 17. What is train-test split?

Divide data.

---

### 18. What is predict()?

Generate output.

---

### 19. What is real-world use?

Spam detection.

---

### 20. Why Iris dataset popular?

Simple + clear separation.

---

# ✅ 7. CLEAN PYTHON IMPLEMENTATION

```python
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

iris = pd.read_csv('Iris.csv')

iris.drop(columns="Id", inplace=True)

sns.relplot(x='SepalLengthCm', y='SepalWidthCm', data=iris, hue='Species', style='Species')

sns.violinplot(x='Species', y='PetalLengthCm', data=iris)

iris.plot.area(y=['SepalLengthCm','SepalWidthCm','PetalLengthCm','PetalWidthCm'])

sns.heatmap(iris.drop(columns='Species').corr(), annot=True)

X = iris.iloc[:,0:4].values
y = iris.iloc[:,4].values

from sklearn.preprocessing import LabelEncoder
le = LabelEncoder()
y = le.fit_transform(y)

from sklearn.model_selection import train_test_split
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.3, random_state=0)

from sklearn.naive_bayes import GaussianNB
gaussian = GaussianNB()

gaussian.fit(X_train, y_train)

Y_pred = gaussian.predict(X_test)

from sklearn.metrics import confusion_matrix, accuracy_score, precision_score, recall_score, f1_score

cm = confusion_matrix(y_test, Y_pred)

print(cm)
print(accuracy_score(y_test, Y_pred))
print(precision_score(y_test, Y_pred, average='micro'))
print(recall_score(y_test, Y_pred, average='micro'))
print(f1_score(y_test, Y_pred, average='micro'))
```

---


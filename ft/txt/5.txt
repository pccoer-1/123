
# ✅ 1. START FROM ZERO (ABSOLUTE BASICS)

## 🔹 What is Classification?

Classification = predicting **category (YES/NO)**

Example:

```text
Will user buy product? → Yes / No
Email spam? → Spam / Not Spam
```

---

## 🔹 What is Logistic Regression?

Even though name says "regression":

👉 It is used for **classification**

---

## 🔹 Output of Logistic Regression

It gives probability:

```text
0 → No
1 → Yes
```

---

## 🔹 Example Dataset (Your Case)

| Age | Salary | Purchased |
| --- | ------ | --------- |
| 19  | 19000  | 0         |
| 35  | 20000  | 0         |

👉 Goal:
Predict **Purchased (0 or 1)**

---

# ✅ 2. WHAT THIS EXPERIMENT DOES

### 🎯 Aim:

1. Train classification model
2. Predict purchase behavior
3. Evaluate using confusion matrix
4. Calculate accuracy, precision, recall

---

### 🌍 Real-world Use:

* Marketing (who will buy?)
* Fraud detection
* Medical diagnosis

---

# ✅ 3. FULL CODE (WORD-BY-WORD EXPLANATION)

---

## 🔹 Import Libraries

```python
import numpy as np
```

* Numerical operations

---

```python
import matplotlib.pyplot as plt
```

* Graph plotting

---

```python
import pandas as pd
```

* Data handling

---

## 🔹 Load Dataset

```python
dataset = pd.read_csv('Social_Network_Ads.csv')
```

### Word-by-word:

* `read_csv` → read file
* `dataset` → stored data

---

## 🔹 Check Data

```python
dataset.head()
```

👉 Shows first rows

---

```python
dataset.info()
```

👉 Shows:

* columns
* data types
* null values

---

```python
dataset.isnull().sum()
```

👉 Check missing values

---

```python
dataset.shape
```

👉 Output:

```text
(400, 5)
```

👉 400 rows, 5 columns

---

# 🔹 Select Features (IMPORTANT)

```python
x = dataset.iloc[:, [2, 3]].values
```

### Word-by-word:

* `iloc` → index selection
* `:` → all rows
* `[2,3]` → Age & Salary
* `.values` → convert to array

👉 x = input features

---

```python
y = dataset.iloc[:, 4].values
```

👉 Output column (Purchased)

---

# 🔹 Split Data

```python
from sklearn.model_selection import train_test_split
```

---

```python
x_train, x_test, y_train, y_test = train_test_split(x, y, test_size=0.30, random_state=0)
```

### Meaning:

* 70% → training
* 30% → testing

---

# 🔹 Feature Scaling (VERY IMPORTANT)

```python
from sklearn.preprocessing import StandardScaler
```

---

```python
sc = StandardScaler()
```

👉 Create scaler

---

```python
x_train = sc.fit_transform(x_train)
```

### Word-by-word:

* `fit` → learn mean & std
* `transform` → scale data

👉 Makes data:

```text
mean = 0
std = 1
```

---

```python
x_test = sc.transform(x_test)
```

👉 Only transform test data

---

# 🔹 Create Model

```python
from sklearn.linear_model import LogisticRegression
```

---

```python
classifier = LogisticRegression(random_state=0)
```

👉 Create model

---

# 🔹 Train Model

```python
classifier.fit(x_train, y_train)
```

👉 Learn patterns

---

# 🔹 Prediction

```python
y_pred = classifier.predict(x_test)
```

👉 Predict outputs

---

```python
print(y_pred)
print(y_test)
```

👉 Compare predicted vs actual

---

# 🔹 Confusion Matrix (MOST IMPORTANT)

```python
from sklearn.metrics import confusion_matrix
cm = confusion_matrix(y_test, y_pred)
print(cm)
```

---

### Output:

```text
[[74 5]
 [11 30]]
```

---

### Meaning:

| Actual \ Predicted | 0       | 1       |
| ------------------ | ------- | ------- |
| 0                  | 74 (TN) | 5 (FP)  |
| 1                  | 11 (FN) | 30 (TP) |

---

# 🔹 Metrics Calculation

---

## ✅ Accuracy

```python
Accuracy = (TP + TN) / Total
```

```text
(74+30)/120 = 0.86
```

👉 86% correct

---

## ❌ Error Rate

```python
Error = (FP + FN) / Total
```

👉 13% wrong

---

## 🔹 Precision

```python
Precision = TP / (TP + FP)
```

👉 85% correct positive predictions

---

## 🔹 Recall

```python
Recall = TP / (TP + FN)
```

👉 73% actual positives found

---

## 🔹 F1 Score

```python
F1 = 2 * (Precision * Recall) / (Precision + Recall)
```

👉 Balance metric

---

# ✅ 4. CORE CONCEPTS (CLEAR)

---

## 🔹 Logistic Function

```text
Output = 1 / (1 + e^-x)
```

👉 Converts value → probability

---

## 🔹 Confusion Matrix

| Term | Meaning     |
| ---- | ----------- |
| TP   | Correct YES |
| TN   | Correct NO  |
| FP   | Wrong YES   |
| FN   | Wrong NO    |

---

## 🔹 Why Scaling?

Without scaling:
👉 Salary (100000) dominates Age (30)

---

# ✅ 5. OUTPUT UNDERSTANDING

* Accuracy = 86%
* Good model
* Some false predictions

---

# ⚠ Common Mistakes

1. Not scaling data
2. Confusing FP and FN
3. Wrong feature selection
4. Not splitting data
5. Misinterpreting metrics

---

# ✅ 6. VIVA QUESTIONS (DETAILED)

---

### 1. What is classification?

Predicting categories.

---

### 2. What is logistic regression?

Classification algorithm.

---

### 3. Output range?

0 to 1.

---

### 4. What is sigmoid function?

Converts to probability.

---

### 5. What is confusion matrix?

Performance table.

---

### 6. What is TP?

Correct positive.

---

### 7. What is TN?

Correct negative.

---

### 8. What is FP?

Wrong positive.

---

### 9. What is FN?

Wrong negative.

---

### 10. What is accuracy?

Correct predictions ratio.

---

### 11. What is precision?

Correctness of positive predictions.

---

### 12. What is recall?

Coverage of actual positives.

---

### 13. What is F1 score?

Balance metric.

---

### 14. Why scaling needed?

To normalize data.

---

### 15. What is overfitting?

Model memorizes data.

---

### 16. What is dataset?

Collection of data.

---

### 17. What is feature?

Input variable.

---

### 18. What is label?

Output variable.

---

### 19. What is predict()?

Generate predictions.

---

### 20. Real-world use?

Marketing, fraud detection.

---

# ✅ 7. CLEAN PYTHON IMPLEMENTATION

```python
import numpy as np
import matplotlib.pyplot as plt
import pandas as pd

dataset = pd.read_csv('Social_Network_Ads.csv')

x = dataset.iloc[:, [2, 3]].values
y = dataset.iloc[:, 4].values

from sklearn.model_selection import train_test_split

x_train, x_test, y_train, y_test = train_test_split(x, y, test_size=0.30, random_state=0)

from sklearn.preprocessing import StandardScaler

sc = StandardScaler()
x_train = sc.fit_transform(x_train)
x_test = sc.transform(x_test)

from sklearn.linear_model import LogisticRegression

classifier = LogisticRegression(random_state=0)
classifier.fit(x_train, y_train)

y_pred = classifier.predict(x_test)

from sklearn.metrics import confusion_matrix, accuracy_score, precision_score, recall_score, f1_score

cm = confusion_matrix(y_test, y_pred)

print(cm)
print(accuracy_score(y_test, y_pred))
print(precision_score(y_test, y_pred))
print(recall_score(y_test, y_pred))
print(f1_score(y_test, y_pred))
```

